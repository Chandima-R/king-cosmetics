export type VisionMission = {
    badge: string;
    title: string;
    subtitle: string;

    vision: {
        title: string;
        description: string;
    };

    mission: {
        title: string;
        description: string;
    };

    quote: string;
};